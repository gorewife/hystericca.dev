{
  "name": "neocities-site",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/main.js --outdir ./dist --minify",
    "dev": "bun build src/main.js --outdir ./dist --watch",
    "serve": "bun run server.js",
    "deploy": "bun run build && wrangler pages deploy . --project-name=hystericca"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "gsap": "^3.14.2"
  }
}
